# Starting point: advanced ML, no GNN exposure

The user has a strong ML background (CNNs, RNNs, backpropagation, optimisation) but no prior GNN experience. Teaching can skip neural network basics entirely and use CNN/RNN analogies as anchoring points. The zone of proximal development starts at: what makes graph-structured data different from grids/sequences, and why that demands a new architecture family.
